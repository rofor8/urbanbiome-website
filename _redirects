# Serve the base wiki index if someone goes to /wiki/
/wiki/    /wiki/index.html   200

# Serve specific intervention pages using wiki/index.html, passing ID as query param
/wiki/:id /wiki/index.html?id=:id   200

# SPA fallback MUST be the last rule
/*        /index.html      200 